# Production Readiness

Run:

```bash
python -m agent.main --mode production-audit
```

This validates config parseability, requirements integrity, workflow presence, artifact ignore rules, and no-secret-safe defaults.
